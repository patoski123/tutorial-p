# Schema
#
# Used as the source of truth for your tests.
#
# You can:
#
# Validate that responses conform to the schema (fields, types, required values).
#
# Auto-generate baseline tests from the schema (contract testing).
#
# Detect breaking changes when the schema changes.